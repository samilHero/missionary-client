PR을 생성해줘.

## 절차

1. `git rev-parse --abbrev-ref HEAD`로 현재 브랜치명을 확인한다.
2. `git log --oneline dev..HEAD`로 포함될 커밋 목록을 확인한다.
3. `git diff dev...HEAD --stat`으로 변경된 파일 목록을 확인한다.
4. `git diff dev...HEAD`로 전체 diff를 분석한다.
5. 커밋 메시지와 diff를 기반으로 PR 제목과 본문을 작성한다.
6. `gh pr create --base dev --head <현재브랜치>` 명령으로 PR을 생성한다.

## PR 본문 형식

```
## Summary
<변경 사항을 1-3개 bullet point로 요약>

## 주요 변경 사항
<카테고리별로 구분하여 상세 설명>

## Test plan
<체크리스트 형태의 테스트 항목>

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

## 규칙

- PR 제목은 70자 이내로 간결하게 작성한다.
- 커밋 메시지에 Jira 티켓 번호(SMH-XX)가 있으면 PR 제목에도 포함한다.
- base 브랜치는 `dev`로 설정한다.
- 본문은 한국어로 작성한다.
- PR 생성 후 URL을 출력한다.
