/**
 * @description
 * 프로젝트 루트에 있는 frontend.js에 해당되는 규칙을 테스트합니다.
 *
 * @example
 * 1. 각 rules에 대한 모듈을 수정합니다.
 * 2. 수정한 룰에 대한 테스트를 진행합니다.
 *
 * import { useEffect, useState } from 'react'
 *
 * function ReactHooksTest() {
 *   const [text, setText] = useState('')

 *   useEffect(() => {
 *     const message = `hi ${text}`
 *   }, [])
 * }
 */
